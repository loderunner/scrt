import{_ as e,o as t,c as a,d as s}from"./app.daf49d13.js";const i={},o=s(`<h1 id="init" tabindex="-1"><a class="header-anchor" href="#init" aria-hidden="true">#</a> init</h1><div class="language-text ext-text"><pre class="language-text"><code>scrt init [flags]
</code></pre></div><p>Initialize a new store. If an item is already present at the given location, the initialization will fail unless the <code>--overwrite</code> option is set.</p><h3 id="options" tabindex="-1"><a class="header-anchor" href="#options" aria-hidden="true">#</a> Options</h3><p><strong><code>--overwrite</code>:</strong> when this flag is set, <code>scrt</code> will overwrite the item at the given location, if it exists, instead of returning an error. If no item exists at the location, <code>--overwrite</code> has no effect.</p><h3 id="example" tabindex="-1"><a class="header-anchor" href="#example" aria-hidden="true">#</a> Example</h3><p>Create a store in a <code>store.scrt</code> file in the local filesystem, in the current working directory, using the password <code>&quot;p4ssw0rd&quot;</code>.</p><div class="language-bash ext-sh"><pre class="language-bash"><code>scrt init --storage<span class="token operator">=</span>local --password<span class="token operator">=</span>p4ssw0rd --local-path<span class="token operator">=</span>./store.scrt
</code></pre></div>`,8),r=[o];function n(c,d){return t(),a("div",null,r)}var h=e(i,[["render",n],["__file","init.html.vue"]]);export{h as default};
